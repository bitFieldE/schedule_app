<div class="container-fluid">
 <h1>
    <% if params['action'] == "index" %>
        <%= "現在の日時 : #{ Date.current&.strftime('%Y年%m月%d日') }　スケジュール合計 : #{Schedule.all.count}" %>
    <% else %>
        <%= "現在の日時 : #{ Date.current&.strftime('%Y年%m月%d日') }" %>
    <% end %>
 </h1>
</div>